lauguage: python

python:
  - 3.5

sudo: required
 
dist: trusty

before_install:
 -  sudo apt-get install python3-pip

install:
  - sudo pip3 install -r requirements.txt

script: 
  - python3 -m test.test